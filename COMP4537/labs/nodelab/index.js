'use strict';

const { startServer } = require('./3/server.js');

// http starting on server module
startServer();

console.log("Listening...\nServer is running on http://localhost:8080 at the moment.");